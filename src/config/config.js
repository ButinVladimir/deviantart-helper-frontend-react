/**
 * Config.
 */
export default class Config {
  /**
   * @description
   * The constructor.
   *
   * @param {string} serverUrl - The server URL.
   */
  constructor(
    serverUrl,
  ) {
    this.serverUrl = serverUrl;
  }
}
